# Disable rewrite rules for seller pages
RewriteEngine Off

# Allow direct access to all PHP files
<Files *.php>
    Order allow,deny
    Allow from all
</Files>

# Prevent directory listing
Options -Indexes

